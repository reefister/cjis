services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: postgres 
      POSTGRES_PASSWORD: CJIS 
      POSTGRES_DB: cjis
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: